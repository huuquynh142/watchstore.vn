<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["purchase_invoices_detail/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["purchase_invoices_detail/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Purchase Of Invoices</th>
            <th>Product</th>
            <th>Quantity</th>
            <th>Price</th>
            <th>Name Of Seller</th>
            <th>Created</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $purchase_invoices_detail): ?>
            <tr>
                <td><?php echo $purchase_invoices_detail->id ?></td>
            <td><?php echo $purchase_invoices_detail->purchase_invoices_id ?></td>
            <td><?php echo $purchase_invoices_detail->product_id ?></td>
            <td><?php echo $purchase_invoices_detail->quantity ?></td>
            <td><?php echo $purchase_invoices_detail->price ?></td>
            <td><?php echo $purchase_invoices_detail->name_seller ?></td>
            <td><?php echo $purchase_invoices_detail->created_at ?></td>

                <td><?php echo $this->tag->linkTo(["purchase_invoices_detail/edit/" . $purchase_invoices_detail->id, "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["purchase_invoices_detail/delete/" . $purchase_invoices_detail->id, "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("purchase_invoices_detail/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("purchase_invoices_detail/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("purchase_invoices_detail/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("purchase_invoices_detail/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
